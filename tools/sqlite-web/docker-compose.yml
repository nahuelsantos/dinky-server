services:
  sqlite-web:
    image: ghcr.io/coleifer/sqlite-web:latest
    container_name: sqlite-web
    command: sqlite_web --host=0.0.0.0 --port=8080 /db/game.db
    ports:
      - "3500:8080"
    volumes:
      - ../../the-game/data:/db
    restart: unless-stopped
